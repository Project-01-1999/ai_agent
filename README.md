# Astra

A local AI operating system built with Python and local language models.

Goals:
- Work completely offline when possible
- Use replaceable LLMs
- Keep long-term memory outside the model
- Perform real tasks on Windows
- Learn from user knowledge without retraining