# Installation

## Prerequisites
- Python 3.10+
- pip

## Setup
```bash
git clone <repo-url>
cd <project-folder>
python -m venv venv
source venv/bin/activate  # or venv\Scripts\activate on Windows
pip install -r requirements.txt
